name: Detective E2E

on:
  push:
    paths:
      - "index.html"
      - "tests/detective-e2e.mjs"
      - "tests/detective-v3-e2e.mjs"
      - "tests/site-smoke.mjs"
      - "tests/evening-ux.mjs"
      - "tests/laptop-1366.mjs"
      - "tests/shopping-list.mjs"
      - "tests/today-prep.mjs"
      - "tests/trip-day-resume.mjs"
      - "tests/case-ux.mjs"
      - "tests/desktop-case-ux.mjs"
      - "tests/offline-shell.mjs"
      - "tests/offline-play.mjs"
      - "tests/case-backup-and-update.mjs"
      - "sw.js"
      - "manifest.webmanifest"
      - "assets/case/**"
      - ".github/workflows/detective-e2e.yml"
  workflow_dispatch:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Verify offline shell
        run: node tests/offline-shell.mjs
      - name: Start local site
        run: |
          python3 -m http.server 4173 >/tmp/kostroma-http.log 2>&1 &
          echo $! > /tmp/kostroma-http.pid
          for i in {1..20}; do
            if curl -fsS http://127.0.0.1:4173/index.html >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/kostroma-http.log
          exit 1
      - name: Run full site smoke test
        run: node tests/site-smoke.mjs
      - name: Verify calm evening experience
        run: node tests/evening-ux.mjs
      - name: Verify 1366x768 laptop experience
        run: node tests/laptop-1366.mjs
      - name: Verify editable shopping list
        run: node tests/shopping-list.mjs
      - name: Verify Today dashboard and pre-trip checklist
        run: node tests/today-prep.mjs
      - name: Run detective browser test
        run: node tests/detective-v3-e2e.mjs
      - name: Verify trip-day progress survives reopening
        run: node tests/trip-day-resume.mjs
      - name: Verify spoiler-safe case navigation and reading
        run: node tests/case-ux.mjs
      - name: Verify laptop detective workspace
        run: node tests/desktop-case-ux.mjs
      - name: Verify backup, document reading and mid-case update
        run: node tests/case-backup-and-update.mjs
      - name: Verify actual offline case play
        run: node tests/offline-play.mjs
      - name: Save reveal screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reveal-mobile
          path: test-artifacts/*.png
          if-no-files-found: ignore
